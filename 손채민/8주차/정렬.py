def solution(array, commands):
    # for문에서 결과값 넣을 list 생성
    result = []

    for i in range(len(commands)):
        pass

    answer = [0] * len(result)

    for i in range(len(result)):
        answer[i] = result[i]

    return answer